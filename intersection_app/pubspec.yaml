name: intersection_app
description: "Smart Route Planning — intersection sensor app (Atsign Platform)"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  at_client_flutter: ^1.0.1
  at_client: ^3.11.0
  at_auth: ^3.1.0
  at_commons: ^5.0.0
  path_provider: ^2.1.4
  camera: ^0.11.0
  google_mlkit_object_detection: ^0.15.0
  google_mlkit_image_labeling: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
