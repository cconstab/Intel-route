name: commuter_app
description: "Smart Route Planning — commuter app (Atsign Platform)"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  at_client_flutter: ^1.0.1
  at_client: ^3.11.0
  at_auth: ^3.1.0
  at_commons: ^5.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
